# Pending Ideas

Phase docs that describe a design/idea but have **no implementation started**,
usually because they're blocked on an Owner decision. Different from
`completed/` (done and shipped) and `active/` (currently being implemented).

Move a phase doc back to `active/` once the blocking decision is made and
work actually starts.

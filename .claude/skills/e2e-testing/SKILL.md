---
name: e2e-testing
description: Create or maintain critical Playwright E2E flows for BaSa3D (browse->cart, checkout->order, admin product update, inventory adjustment, custom request submission). Trigger on "write E2E test", "add Playwright test", or when a critical flow changes behavior.
---

Read `.agents/skills/e2e-testing/SKILL.md` at the repo root for the required
flows and rules (stable selectors, no sleeps, seeded data, small smoke tests).
Apply it when writing or updating tests.
